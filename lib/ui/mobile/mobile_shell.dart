/// Placeholder for future mobile chrome (bottom toolbar, touch controls).
///
/// Mobile UI must call the same editor commands / CanvasDocument APIs as
/// desktop — never duplicate editor logic here.
library;

// Intentionally empty in phase 1.
